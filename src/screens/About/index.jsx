import { Stack } from "@mui/material";

export const About = () => {
  return (
    <Stack
      spacing={2}
      sx={{ display: "flex", alignItems: "center", justifyContent: "center" }}
    >
      About
    </Stack>
  );
};
